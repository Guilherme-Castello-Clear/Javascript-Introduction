var num = 8;// Defining var 'num' as 8.
for(let i = 0; i <= 10; i++){ //For's used to make a loop with an index. In this case, the index is "i".
    console.log(`${num} * ${i} = ${num * i}`);// As we can see here, the index can be used inside (and just inside) the for loop.
}
